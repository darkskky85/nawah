# تشغيل نَوَاة في الإنتاج

الموقع يعمل محلياً من دون مفاتيح، لكن النماذج لا تعرض نجاحاً زائفاً. لتفعيل الإرسال الفعلي على Vercel:

1. انسخ متغيرات `.env.example` إلى إعدادات Environment Variables في المشروع.
2. اربط `NEWSLETTER_WEBHOOK_URL` بمنصة النشرة أو مسار n8n يفعّل التأكيد المزدوج ويحفظ المشترك.
3. اضبط `RESEND_API_KEY` وتحقق من نطاق عنوان `CONTACT_FROM_EMAIL` لتصل رسائل التواصل.
4. أنشئ Blob Store واضبط `BLOB_READ_WRITE_TOKEN` إذا كنت تستخدم تسليم Moyasar الموجود في `api/webhook.js`.
5. يجب ضبط `WEBHOOK_SECRET`؛ الكود يوقف التسليم تلقائياً إن كان السر غائباً.
6. راجع روابط الدفع الثلاثة في أول `assets/data.js`. المزود الحالي يعطي رابطاً مستقلاً لكل منتج، لذلك تعرض صفحة `checkout.html` العمليات منفصلة بوضوح ولا تفتح نوافذ تلقائياً.

## تحديث المحتوى

شغّل `npm run build` قبل النشر. خط البناء يقوم تلقائياً بـ:

- توحيد بنية الصفحات ونسخ الملفات؛
- مزامنة وقت القراءة وعدد الكلمات؛
- توليد فهرس البحث العربي؛
- مزامنة `sitemap.xml` مع `dateModified`؛
- فحص 50 مقالاً وكل الروابط المحلية ومتطلبات الثقة والوصول.

إذا تعذر `npm` في جهاز التطوير يمكن تشغيل الأوامر الموجودة داخل `scripts` مباشرة بواسطة Node.js 20 أو أحدث.

## ملاحظة التخزين

السجلات الحالية تستخدم Blob عاماً مع أسماء توكنات عشوائية طويلة. قبل توسيع المبيعات يُفضّل إنشاء Private Blob Store وترقية مسار القراءة إلى `get()` الموثّق من `@vercel/blob`، ثم اختبار التسليم والاستعادة في بيئة تجريبية قبل نقل الملفات الفعلية.
